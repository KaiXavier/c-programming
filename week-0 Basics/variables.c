#include <stdio.h> // basic stuff
int main()
{

    int a, b; //<- variable declarations
    // there are two variables a , b (you can declare as many variables as you want)

    // different variables
    float x, y;

    char c;

    // to give a value to a variable, you have two ways :-
    // 1.
    int b = 5; //<- we declare the variable and assign a value to it, in the same line

    // 2.
    int c; //<- we declare the variable
    c = 5; //<- in the next line we assign the value to it
}